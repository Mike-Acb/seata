registry {
  type = "redis"

  redis {
    serverAddr = "host.docker.internal:6379"
    db = "0"
    password = ""
    cluster = "default"
    timeout = "0"
  }
}

config {
  # file、nacos 、apollo、zk、consul、etcd3、springCloudConfig
  type = "file"
  file {
    name = "file.conf"
  }
}
